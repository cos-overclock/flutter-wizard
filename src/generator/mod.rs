use crate::error::WizardError;

/// Generate a Flutter project from the given configuration.
pub fn generate() -> Result<(), WizardError> {
    println!("generator: not yet implemented");
    Ok(())
}
